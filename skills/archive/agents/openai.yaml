interface:
  display_name: "Xpowers Archive"
  short_description: "Archive a completed plan and merge its stack"
  default_prompt: "Use $archive to archive the completed plan and merge its stack."
policy:
  allow_implicit_invocation: false
